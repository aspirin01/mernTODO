<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    <div class="box" id="heading">
        <h2><%= title%> </h2>
    </div>
        <div class="box">

            <% items.forEach((item)=>{  if(item.name!==""){ %>
            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="checkbox" value="<%= item._id%>" onclick="this.form.submit() ">
                    <p>
                        <%= item.name %>
                    </p>
                    <input type="hidden" name="listName" value="<%= title %>">
                </div> 
            </form>  
            <% }}) %>
            <!-- <% for(var i=0;i<items.length;i++){ if(items[i]!==""){ %>
                <div class="item">
                    <input type="checkbox" name="" id="">
                    <p>
                        <%= items[i].name %>
                    </p>
                </div>
                <% }} %> -->
                <form action="/" method="post" class="item">
                    <input type="text" placeholder="Enter Todo Item" autocomplete="off" name="todoItem">
                    <button type="submit" name="listTitle" value="<%= title%>">+</button>
                </form>
            </div>
</body>
</html>